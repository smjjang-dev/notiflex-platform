apiVersion: v1
kind: Service
metadata:
  name: notiflex-api
  namespace: notiflex
  labels:
    app: notiflex-api
spec:
  selector:
    app: notiflex-api
  ports:
  - name: http
    port: 80
    targetPort: 8080
